<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Supabase credentials — injected from SGO.xcconfig at build time -->
    <!-- NOTE: SUPABASE_HOST is just the hostname (no https://) to avoid xcconfig
         treating // as a comment. APIConfig.swift prepends the scheme. -->
    <key>SUPABASE_HOST</key>
    <string>$(SUPABASE_HOST)</string>
    <key>SUPABASE_ANON_KEY</key>
    <string>$(SUPABASE_ANON_KEY)</string>

    <!-- Local network host for physical device testing — set in SGO.xcconfig -->
    <key>API_HOST</key>
    <string>$(API_HOST)</string>

    <!-- Custom URL scheme for ASWebAuthenticationSession OAuth callback -->
    <!-- AuthManager uses "smartgroceryoptimizer://" as the callbackURLScheme -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>com.sgo.smartgroceryoptimizer.oauth</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>smartgroceryoptimizer</string>
            </array>
        </dict>
    </array>

    <!-- Allow HTTP to localhost (dev server) and local network IP -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <!-- Allows Bonjour / mDNS / link-local HTTP (covers simulator localhost) -->
        <key>NSAllowsLocalNetworking</key>
        <true/>
        <key>NSExceptionDomains</key>
        <dict>
            <!-- Dev server on simulator -->
            <key>localhost</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
                <key>NSIncludesSubdomains</key>
                <false/>
            </dict>
            <!-- Dev server on physical device (API_HOST from SGO.xcconfig) -->
            <key>192.168.0.33</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
                <key>NSIncludesSubdomains</key>
                <false/>
            </dict>
        </dict>
    </dict>

    <!-- Standard iOS app keys (Xcode will merge these with generated values) -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISceneConfigurations</key>
        <dict>
            <key>UIWindowSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key>
                    <string>Default Configuration</string>
                </dict>
            </array>
        </dict>
    </dict>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
</dict>
</plist>
